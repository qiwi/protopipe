import Pipeline from '../../main/pipepine'

describe('pipeline', () => {
  describe('constructor', () => {
    it('returns proper instance', () => {

    })
  })

  describe('proto', () => {})

  describe('static', () => {})
})
